using HsConsole.Game.Object;
using static HsConsole.Game.Enum.HsEnum;

namespace HsConsole.Card.BAS
{
    class MurlocRaider_BAS_Minion : Minion
    {
        public MurlocRaider_BAS_Minion() : base("Murloc Raider", 1, 2, 1, string.Empty, Race.Murloc)
        {
        }
    }
}
